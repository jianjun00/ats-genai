apiVersion: v1
kind: ConfigMap
metadata:
  name: job-runs-migration-script
  namespace: ats-dev
# Script extracted to /scripts/create_job_runs_table.py
# Original ConfigMap data removed for cleanup---
apiVersion: batch/v1
kind: Job
metadata:
  name: create-job-runs-table-job
  namespace: ats-dev
spec:
  template:
    spec:
      containers:
      - name: create-job-runs-table
        image: python:3.12-slim
        command: ["/bin/bash"]
        args:
        - "-c"
        - "/scripts/create_job_runs_table_1.py"