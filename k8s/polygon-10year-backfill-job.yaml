apiVersion: batch/v1
kind: Job
metadata:
  name: polygon-10year-backfill
  namespace: ats-dev
  labels:
    app: ats-genai
    component: polygon-historical-backfill
spec:
  ttlSecondsAfterFinished: 7200
  template:
    spec:
      restartPolicy: Never
      containers:
      - name: polygon-backfill
        image: python:3.12-slim
        command: ["/bin/bash", "-c"]
        args:
        - |
          set -e
          echo "🚀 Starting Polygon 10-year historical backfill..."
          pip install --no-cache-dir asyncpg aiohttp pandas gin-config
          
          mkdir -p /app/src/market_data/eod
          cd /app
          
          # Create enhanced backfill script
          cat > /app/src/market_data/eod/polygon_10year_backfill.py << 'EOF'
          #!/usr/bin/env python3
          """
          Polygon 10-Year Historical Backfill
          Comprehensive backfill for major stocks from 2015-2025
          """
          
          import os
          import asyncio
          import aiohttp
          import asyncpg
          import datetime as dt
          import logging
          from typing import List, Dict, Any, Optional
          from dataclasses import dataclass
          
          @dataclass
          class BackfillProgress:
              total_symbols: int = 0
              completed_symbols: int = 0
              total_records: int = 0
              failed_symbols: List[str] = None
              
              def __post_init__(self):
                  if self.failed_symbols is None:
                      self.failed_symbols = []
          
          class SimpleEnvironment:
              def get_table_name(self, base_name):
                  return f"dev_{base_name}"
              
              def get_database_url(self):
                  host = os.environ.get('DB_HOST', 'postgres-simple')
                  port = os.environ.get('DB_PORT', '5432')
                  user = os.environ.get('DB_USER', 'postgres')
                  password = os.environ.get('DB_PASSWORD', 'dev_password')
                  database = os.environ.get('DB_NAME', 'dev_db')
                  return f"postgresql://{user}:{password}@{host}:{port}/{database}"
          
          class PolygonDAO:
              def __init__(self, env):
                  self.env = env
                  self.table_name = env.get_table_name('daily_prices_polygon')
                  self.db_url = env.get_database_url()
              
              async def batch_insert_prices(self, prices):
                  if not prices:
                      return 0
                  pool = await asyncpg.create_pool(self.db_url, min_size=2, max_size=5)
                  try:
                      async with pool.acquire() as conn:
                          await conn.executemany(f"""
                              INSERT INTO {self.table_name} 
                              (date, instrument_id, open_price, high_price, low_price, close, volume)
                              VALUES ($1, $2, $3, $4, $5, $6, $7)
                              ON CONFLICT (date, instrument_id) DO UPDATE SET
                                  open_price=EXCLUDED.open_price,
                                  high_price=EXCLUDED.high_price,
                                  low_price=EXCLUDED.low_price,
                                  close=EXCLUDED.close,
                                  volume=EXCLUDED.volume,
                                  updated_at=NOW()
                          """,
                          [
                              (
                                  p['date'],
                                  p['instrument_id'],
                                  p.get('open_price'),
                                  p.get('high_price'),
                                  p.get('low_price'),
                                  p.get('close'),
                                  p.get('volume'),
                              ) for p in prices
                          ])
                          return len(prices)
                  finally:
                      await pool.close()
          
          class SimpleXrefsDAO:
              def __init__(self, env):
                  self.env = env
                  self.db_url = env.get_database_url()
              
              async def get_major_symbols_with_ids(self):
                  """Get major symbols with their instrument IDs"""
                  pool = await asyncpg.create_pool(self.db_url, min_size=1, max_size=3)
                  try:
                      async with pool.acquire() as conn:
                          rows = await conn.fetch("""
                              SELECT DISTINCT 
                                  i.id as instrument_id,
                                  i.symbol,
                                  ix.vendor_symbol
                              FROM dev_instruments i
                              JOIN dev_instrument_xrefs ix ON i.id = ix.instrument_id
                              JOIN dev_vendors v ON ix.vendor_id = v.id
                              WHERE v.name = 'polygon'
                                AND i.symbol IN (
                                  'AAPL', 'MSFT', 'GOOGL', 'AMZN', 'TSLA', 'META', 'NVDA', 'BRK.A', 'BRK.B',
                                  'JPM', 'JNJ', 'V', 'PG', 'HD', 'MA', 'UNH', 'DIS', 'BAC', 'ADBE', 'CRM',
                                  'NFLX', 'XOM', 'CVX', 'PFE', 'KO', 'PEP', 'ABBV', 'MRK', 'WMT', 'COST',
                                  'TMO', 'ACN', 'MDT', 'DHR', 'NEE', 'VZ', 'T', 'CMCSA', 'NKE', 'HON'
                                )
                              ORDER BY i.symbol
                              LIMIT 50
                          """)
                          return [(row['instrument_id'], row['symbol'], row['vendor_symbol']) for row in rows]
                  finally:
                      await pool.close()
          
          # Polygon API configuration
          POLYGON_API_KEY = os.getenv("POLYGON_API_KEY")
          POLYGON_BASE_URL = "https://api.polygon.io"
          
          logger = logging.getLogger(__name__)
          
          async def fetch_polygon_historical_data(session: aiohttp.ClientSession, symbol: str, 
                                                start_date: dt.date, end_date: dt.date) -> List[Dict[str, Any]]:
              """Fetch historical data from Polygon API with proper date chunking"""
              if not POLYGON_API_KEY:
                  logger.error("POLYGON_API_KEY not available")
                  return []
              
              url = f"{POLYGON_BASE_URL}/v2/aggs/ticker/{symbol}/range/1/day/{start_date}/{end_date}"
              params = {
                  'adjusted': 'true',
                  'sort': 'asc',
                  'limit': 50000,  # Max allowed
                  'apikey': POLYGON_API_KEY
              }
              
              try:
                  async with session.get(url, params=params) as response:
                      if response.status == 429:
                          logger.warning(f"Rate limit exceeded for {symbol}")
                          await asyncio.sleep(60)
                          return []
                      
                      if response.status != 200:
                          error_text = await response.text()
                          logger.error(f"Polygon API error for {symbol}: {response.status} - {error_text}")
                          return []
                      
                      data = await response.json()
                      
                      if data.get('status') != 'OK':
                          logger.error(f"Polygon error for {symbol}: {data.get('status')}")
                          return []
                      
                      if 'results' not in data or not data['results']:
                          logger.warning(f"No results for {symbol} from {start_date} to {end_date}")
                          return []
                      
                      prices = []
                      for result in data['results']:
                          try:
                              # Convert Unix timestamp to date
                              date_obj = dt.date.fromtimestamp(result['t'] / 1000)
                              
                              open_price = float(result.get('o', 0))
                              high_price = float(result.get('h', 0))
                              low_price = float(result.get('l', 0))
                              close_price = float(result.get('c', 0))
                              volume = int(result.get('v', 0))
                              
                              price_record = {
                                  'date': date_obj,
                                  'open_price': open_price,
                                  'high_price': high_price,
                                  'low_price': low_price,
                                  'close': close_price,
                                  'volume': volume
                              }
                              
                              prices.append(price_record)
                              
                          except (ValueError, KeyError) as e:
                              logger.warning(f"Error parsing data for {symbol}: {e}")
                              continue
                      
                      logger.info(f"Fetched {len(prices)} records for {symbol} ({start_date} to {end_date})")
                      return prices
                      
              except aiohttp.ClientError as e:
                  logger.error(f"Connection error fetching {symbol}: {e}")
                  return []
          
          async def backfill_symbol_in_chunks(session: aiohttp.ClientSession, dao: PolygonDAO,
                                            symbol: str, vendor_symbol: str, instrument_id: int,
                                            start_date: dt.date, end_date: dt.date,
                                            progress: BackfillProgress) -> bool:
              """Backfill a symbol in yearly chunks to handle large date ranges"""
              
              logger.info(f"🔄 Starting backfill for {symbol} ({vendor_symbol}) from {start_date} to {end_date}")
              
              current_date = start_date
              symbol_total_records = 0
              
              while current_date < end_date:
                  # Process in 1-year chunks
                  chunk_end = min(current_date.replace(year=current_date.year + 1), end_date)
                  
                  logger.info(f"  📅 Processing {symbol}: {current_date} to {chunk_end}")
                  
                  prices = await fetch_polygon_historical_data(session, vendor_symbol, current_date, chunk_end)
                  
                  if prices:
                      # Add instrument_id to all records
                      for price in prices:
                          price['instrument_id'] = instrument_id
                      
                      records_inserted = await dao.batch_insert_prices(prices)
                      symbol_total_records += records_inserted
                      progress.total_records += records_inserted
                      
                      logger.info(f"  ✅ Inserted {records_inserted} records for {symbol} ({current_date} to {chunk_end})")
                  else:
                      logger.warning(f"  ⚠️  No data for {symbol} ({current_date} to {chunk_end})")
                  
                  current_date = chunk_end
                  
                  # Rate limiting: 5 calls per minute for basic plan
                  await asyncio.sleep(12)  # 12 seconds between calls = 5 calls/minute
              
              logger.info(f"✅ Completed {symbol}: {symbol_total_records} total records")
              return symbol_total_records > 0
          
          async def main():
              logging.basicConfig(level=logging.INFO, format='%(asctime)s - %(levelname)s - %(message)s')
              
              if not POLYGON_API_KEY:
                  print("❌ POLYGON_API_KEY environment variable not set")
                  return
              
              print("🚀 Starting Polygon 10-year historical backfill...")
              print(f"📅 Date range: 2015-01-01 to 2025-08-19")
              
              env = SimpleEnvironment()
              dao = PolygonDAO(env)
              xrefs_dao = SimpleXrefsDAO(env)
              
              # Get symbols to backfill
              symbols_data = await xrefs_dao.get_major_symbols_with_ids()
              
              if not symbols_data:
                  print("❌ No symbols found for backfill")
                  return
              
              progress = BackfillProgress(total_symbols=len(symbols_data))
              
              print(f"📊 Found {len(symbols_data)} symbols to backfill")
              
              start_date = dt.date(2015, 1, 1)
              end_date = dt.date(2025, 8, 19)
              
              timeout = aiohttp.ClientTimeout(total=60)
              async with aiohttp.ClientSession(timeout=timeout) as session:
                  for instrument_id, symbol, vendor_symbol in symbols_data:
                      try:
                          success = await backfill_symbol_in_chunks(
                              session, dao, symbol, vendor_symbol, instrument_id,
                              start_date, end_date, progress
                          )
                          
                          if success:
                              progress.completed_symbols += 1
                          else:
                              progress.failed_symbols.append(symbol)
                          
                          # Progress update
                          completion_pct = (progress.completed_symbols / progress.total_symbols) * 100
                          print(f"📈 Progress: {progress.completed_symbols}/{progress.total_symbols} ({completion_pct:.1f}%) - Total records: {progress.total_records}")
                          
                      except Exception as e:
                          logger.error(f"❌ Failed to backfill {symbol}: {e}")
                          progress.failed_symbols.append(symbol)
              
              # Final summary
              print(f"\n🎉 Polygon backfill completed!")
              print(f"✅ Successfully processed: {progress.completed_symbols}/{progress.total_symbols} symbols")
              print(f"📊 Total records inserted: {progress.total_records:,}")
              
              if progress.failed_symbols:
                  print(f"❌ Failed symbols: {', '.join(progress.failed_symbols)}")
          
          if __name__ == "__main__":
              asyncio.run(main())
          EOF
          
          echo "🔄 Running Polygon 10-year backfill..."
          cd /app && python src/market_data/eod/polygon_10year_backfill.py
        env:
        - name: POLYGON_API_KEY
          valueFrom:
            secretKeyRef:
              name: api-credentials-dev
              key: POLYGON_API_KEY
        - name: DB_HOST
          value: "postgres-simple"
        - name: DB_PORT
          value: "5432"
        - name: DB_USER
          value: "postgres"
        - name: DB_PASSWORD
          value: "dev_password"
        - name: DB_NAME
          value: "dev_db"
        - name: PYTHONPATH
          value: "/app/src"
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "1000m"