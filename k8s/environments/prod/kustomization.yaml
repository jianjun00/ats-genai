apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ats-prod

resources:
- ../../base

patchesStrategicMerge:
- deployment-patch.yaml

images:
- name: ats-genai
  newTag: prod-latest

commonLabels:
  environment: prod
