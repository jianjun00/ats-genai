apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ats-dev

resources:
- ../../base

patchesStrategicMerge:
- deployment-patch.yaml

images:
- name: ats-genai
  newTag: dev-latest

commonLabels:
  environment: dev
