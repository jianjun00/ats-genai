apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- deployment.yaml
- service.yaml
- configmap.yaml
- secrets.yaml

commonLabels:
  app.kubernetes.io/name: ats-genai
  app.kubernetes.io/component: api

images:
- name: ats-genai
  newTag: latest
