apiVersion: v1
kind: Service
metadata:
  name: ats-api-service
  labels:
    app: ats-api
spec:
  selector:
    app: ats-api
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  type: ClusterIP
