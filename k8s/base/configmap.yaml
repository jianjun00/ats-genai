apiVersion: v1
kind: ConfigMap
metadata:
  name: ats-config
data:
  # Application configuration
  LOG_LEVEL: "INFO"
  PYTHONPATH: "src"
  # Database configuration (non-sensitive)
  DB_POOL_SIZE: "10"
  DB_MAX_OVERFLOW: "20"
  # API configuration
  API_TIMEOUT: "30"
  MAX_WORKERS: "4"
