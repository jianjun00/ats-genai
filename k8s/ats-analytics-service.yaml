apiVersion: v1
kind: ConfigMap
metadata:
  name: ats-analytics-config
  namespace: ats-dev
# Script extracted to /scripts/main.py
# Original ConfigMap data removed for cleanup
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ats-analytics-service
  namespace: ats-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ats-analytics-service
  template:
    metadata:
      labels:
        app: ats-analytics-service
    spec:
      containers:
      - name: analytics
        image: python:3.9-slim
        ports:
        - containerPort: 3000
        env:
        - name: PYTHONUNBUFFERED
          value: "1"
        - name: GITHUB_TOKEN
          valueFrom:
            secretKeyRef:
              name: github-credentials
              key: token
        command: ["/bin/bash"]
        args:
        - "-c"
        - |
          echo "📦 Installing git..."
          apt-get update && apt-get install -y git
          
          echo "📦 Cloning source code from Git with credentials..."
          git clone https://${GITHUB_TOKEN}:x-oauth-basic@github.com/AkoloTechnologies/ats-genai.git /app/src-repo
          cd /app/src-repo
          
          echo "🚀 Starting analytics service..."
          PYTHONPATH=/app/src-repo/src python /app/src-repo/scripts/k8s-extracted/main.py
---
apiVersion: v1
kind: Service
metadata:
  name: ats-analytics-service
  namespace: ats-dev
spec:
  type: NodePort
  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 30001
  selector:
    app: ats-analytics-service