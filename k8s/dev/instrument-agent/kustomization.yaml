apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ats-dev

resources:
  - configmap.yaml
  - secret.yaml
  - cronjob.yaml
  - job.yaml
  - pvc.yaml

commonLabels:
  app: instrument-agent
  environment: dev
