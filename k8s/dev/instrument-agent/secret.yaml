apiVersion: v1
kind: Secret
metadata:
  name: instrument-agent-secrets
  namespace: ats-dev
type: Opaque
data:
  # These are base64-encoded placeholders - replace with actual values in your environment
  # Use: echo -n "your-actual-value" | base64
  DB_USER: cG9zdGdyZXM=  # postgres
  DB_PASSWORD: cGFzc3dvcmQ=  # password
  # IMPORTANT: Replace these with your actual API keys (base64 encoded)
  # DO NOT commit actual API keys to Git
  POLYGON_API_KEY: UkVQTEFDRV9XSVRIX1lPVVJfQUNUVUFMX0tFWQ==  # REPLACE_WITH_YOUR_ACTUAL_KEY
  # Add other API keys as needed, but use placeholders only
