apiVersion: batch/v1
kind: Job
metadata:
  name: run-price-unification-migration
  namespace: ats-dev
spec:
  template:
    spec:
      restartPolicy: Never
      containers:
      - name: migration
        image: python:3.11-slim
        command: ["/bin/bash"]
        args:
        - "-c"
        - "/scripts/run_price_unification_migration_1.py"