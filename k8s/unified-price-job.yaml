apiVersion: v1
kind: ConfigMap
metadata:
  name: unified-price-job-script
  namespace: ats-dev
# Script extracted to /scripts/environment.py
# Original ConfigMap data removed for cleanup---
apiVersion: batch/v1
kind: Job
metadata:
  name: unified-price-job
  namespace: ats-dev
spec:
  template:
    spec:
      containers:
      - name: price-unifier
        image: python:3.12-slim
        command: ["/bin/bash"]
        args:
        - "-c"
        - "/scripts/unified_price_job_1.py"