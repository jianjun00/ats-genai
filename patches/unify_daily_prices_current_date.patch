--- a/src/market_data/eod/unify_daily_prices.py
+++ b/src/market_data/eod/unify_daily_prices.py
@@ class DatabaseDailyPricesUnifier(DailyPricesUnifierBase):
-    async def unify_daily_prices(self, symbol, asof):
+    async def unify_daily_prices(self, symbol, asof, current_date):
@@ class FileDailyPricesUnifier(DailyPricesUnifierBase):
-    async def unify_daily_prices(self, symbol, asof):
+    async def unify_daily_prices(self, symbol, asof, current_date):
@@ if __name__ == "__main__":
-    asyncio.run(unifier.unify_daily_prices(args.symbol, (args.start_date, args.end_date)))
+    from datetime import date
+    current_date = date.today()  # or pass from CLI if needed
+    asyncio.run(unifier.unify_daily_prices(args.symbol, (args.start_date, args.end_date), current_date))
