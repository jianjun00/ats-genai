# Local LLM Dependencies for Self-Hosted Models
# Core deep learning and transformer libraries

# PyTorch and CUDA support
torch>=2.1.0
torchvision>=0.16.0
torchaudio>=2.1.0

# Transformers and model libraries
transformers>=4.36.0
accelerate>=0.25.0
peft>=0.7.0  # Parameter-Efficient Fine-Tuning
bitsandbytes>=0.41.0  # Quantization support

# Tokenization and text processing
tokenizers>=0.15.0
sentencepiece>=0.1.99

# GPU and system monitoring
gpustat>=1.1.1
GPUtil>=1.4.0
psutil>=5.9.0
nvidia-ml-py3>=7.352.0

# Model serving and API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0

# Scientific computing and utilities
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.3.0

# HTTP client for model downloads
requests>=2.31.0
aiohttp>=3.9.0

# Configuration and utilities
pyyaml>=6.0.1
python-dotenv>=1.0.0
rich>=13.0.0  # Better console output

# Quantization libraries (optional)
optimum>=1.15.0
auto-gptq>=0.5.0  # GPTQ quantization
autoawq>=0.1.8  # AWQ quantization

# Model optimization
einops>=0.7.0  # Einstein notation for tensor operations
flash-attn>=2.3.0  # Flash attention (if supported)

# Monitoring and logging
prometheus-client>=0.19.0
structlog>=23.2.0

# Financial domain specific (optional)
yfinance>=0.2.0  # For financial data context
pandas>=2.1.0  # Data manipulation

# Development and testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.25.0  # Async HTTP client for testing

# Memory optimization
safetensors>=0.4.0  # Safe tensor serialization
datasets>=2.15.0  # Hugging Face datasets

# Model caching and storage
huggingface-hub>=0.19.0
diskcache>=5.6.0

# Optional: Ollama integration for easy model management
# ollama>=0.1.0

# Optional: vLLM for high-performance inference (requires specific GPU)
# vllm>=0.2.0

# Optional: Text generation inference server
# text-generation-inference>=1.0.0