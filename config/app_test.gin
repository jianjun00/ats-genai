quandl_api_key = 'REPLACE_WITH_ACTUAL_QUANDL_API_KEY'
fmp_api_key = 'REPLACE_WITH_ACTUAL_FMP_API_KEY'
polygon_api_key = 'REPLACE_WITH_ACTUAL_POLYGON_API_KEY'
tiingo_api_key = 'REPLACE_WITH_ACTUAL_TIINGO_API_KEY'

# Logging Configuration



# Features
bind = ['enable_caching', False]
bind = ['enable_metrics', False]
bind = ['strict_validation', True]

# import app.indicator_runner  # Temporarily disabled due to protobuf dependency issues
# Use the correct environment import path  
import src.core.platform.config.environment
# import signals.indicator  # Temporarily disabled due to dependency on app.indicator_runner

# Configure the environment for testing
# Define macros for environment types
TEST = "TEST"

src.core.platform.config.environment.Environment.env_type = %TEST
src.core.platform.config.environment.Environment.db_url = "postgresql://test_user:test_password@localhost:5432/test_db"

# Logging config - make it optional since it causes import issues
# shared.data_handling.utils.environment.Environment.logging_config = @core.config.logging_config.LoggingConfig()
# core.config.logging_config.LoggingConfig.log_level = "DEBUG"
# core.config.logging_config.LoggingConfig.log_format = "%(asctime)s - %(name)s - %(levelname)s - %(message)s"

# Configure the indicator runner (temporarily disabled due to dependency issues)
# app.indicator_runner.IndicatorRunner.indicator_config = [
#     @signals.indicator.EnvelopeTop(),
#     @signals.indicator.EnvelopeBot(),
#     @signals.indicator.PL(),
# ]
