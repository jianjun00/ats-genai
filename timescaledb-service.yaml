---
# TimescaleDB Service Alias
apiVersion: v1
kind: Service
metadata:
  name: timescaledb
  namespace: ats-dev
  labels:
    app: postgres
spec:
  ports:
  - port: 5432
    targetPort: 5432
  selector:
    app: postgres
